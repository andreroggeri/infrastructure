apiVersion: v1
kind: Service
metadata:
  name: {{ include "actual-http-api.fullname" . }}
  labels:
    {{- include "actual-http-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actual-http-api.selectorLabels" . | nindent 4 }}
 