version: "2.4"

networks:
  applications:
    external: true

services:
  wyze-bridge:
    container_name: wyze-bridge
    restart: unless-stopped
    image: mrlt8/wyze-bridge:2.4.0
    networks:
      - applications
    environment:
      - API_ID=${WYZE_API_ID}
      - API_KEY=${WYZE_API_KEY}
